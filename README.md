# MyMolt 記帳網頁

一個輕量、現代化的記帳 Web App（React + TypeScript + Tailwind）。

## 功能（MVP）
- 新增收入/支出：日期、分類、金額、備註
- 明細列表：搜尋、篩選、刪除
- 統計：收入/支出/結餘
- 本機保存：localStorage（不需要登入）

## 開發
```bash
npm install
npm run dev
```

## 公網測試（Codespaces）
```bash
npm run dev:public
```
預設使用 `18888` port（避免和既有 18789 衝突）。
